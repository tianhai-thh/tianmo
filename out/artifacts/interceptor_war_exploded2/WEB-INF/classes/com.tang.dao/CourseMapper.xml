<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.tang.dao.CourseMapper">
<!--
    <select id="queryAllCourse" resultType="com.tang.pojo.Course">
        select * from mybatis.course ;
    </select>

    <select id="queryById" resultType="Course" >
        select * from mybatis.course where CourseId = #{CourseId};
    </select>

    <select id="queryCourseByName" resultType="Course">
#         select * from course where mybatis.school.schoolname = #{schoolname};
        select CourseId,name,hours,sid from mybatis.course, mybatis.school where
            sid=school.id and school.schoolname=#{schoolname};
    </select>

    <insert id="addCourse" useGeneratedKeys="true" keyProperty="CourseId" parameterType="Course">
        insert into mybatis.course(name, hours, sid)
        values (#{name}, #{hours}, #{sid});
    </insert>

    <update id="updateCourse">
        update mybatis.course
        set name=#{name},hours=#{hours},sid=#{sid}
        where CourseId=#{CourseId};
    </update>

    <delete id="deleteCourseById" parameterType="int">
        delete from mybatis.course
        where CourseId = #{CourseId};
    </delete>-->


</mapper>